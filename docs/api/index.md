# API Reference

Auto-generated reference for the `wsi_pipeline` package. Heavy optional
dependencies (Torch, TensorStore, Dask, OpenCV, Neuroglancer, MLflow, …) are
mocked at build time, so signatures render without the full runtime stack
installed.

```{eval-rst}
.. autosummary::
   :toctree: _autosummary
   :template: autosummary/module.rst
   :recursive:

   wsi_pipeline
```

## Planned / emerging modules

Some namespaces are still taking shape and are intentionally **not** listed above
until they ship importable source:

- `wsi_pipeline.submission` — batch/job submission helpers (no public source yet).

Test-only modules (e.g. `wsi_pipeline.sciserver.test_sciserver_modules`) are
excluded from the reference on purpose.

## Indices

- {ref}`genindex`
- {ref}`modindex`
- {ref}`search`

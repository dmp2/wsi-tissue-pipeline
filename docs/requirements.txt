# Documentation build dependencies for the WSI Tissue Pipeline Sphinx site.
#
# Install with:  pip install -r docs/requirements.txt
# The package itself is installed separately with:  pip install --no-deps -e .
#
# Note: nbsphinx also requires the `pandoc` binary, which is NOT a pip package.
# Install it via your OS package manager (apt-get install -y pandoc) or conda
# (see docs/environment.yml).

# --- Sphinx doc-build stack ---
sphinx>=7.0
sphinx_rtd_theme>=2.0
myst-parser>=2.0
linkify-it-py>=2.0
nbsphinx>=0.9
nbsphinx-link>=1.3
ipython>=8.0
sphinx-copybutton>=0.5

# --- Lightweight core package deps (installed, not mocked, for autodoc fidelity) ---
numpy>=1.24
pydantic>=2.0
pydantic-settings>=2.0
PyYAML>=6.0
click>=8.1
rich>=13.0
packaging>=23.0
platformdirs>=4.2
filelock>=3.16
python-dotenv>=1.0
tqdm>=4.65
